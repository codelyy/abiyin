import React, { memo } from 'react'
import Header from '../../component/header/Header'

const Home = memo(() => {
    return (
        <div>
            <Header />
        </div>
    )
})

export default Home